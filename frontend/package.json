{
  "name": "refinet-cloud-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rm -rf .next && NODE_OPTIONS='--max-old-space-size=512' next dev -p 3002",
    "build": "rm -rf .next && next build",
    "start": "next start",
    "export": "rm -rf .next && next build",
    "clean": "rm -rf .next node_modules/.cache"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "framer-motion": "^12.38.0",
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.5.0"
  }
}
